base: roblox
name: selene_defs
globals:
  # override Roblox require style with string requires
  require:
    args:
    - type: string

  # changes assert rules to allow optional second arg
  assert:
    args:
      - type: any
      - type: string
        required: false